# Uncomment the following lines to enable the Corne RGB Underglow
# CONFIG_ZMK_RGB_UNDERGLOW=y
# CONFIG_WS2812_STRIP=y

# Uncomment the following line to enable the Corne OLED Display
# CONFIG_ZMK_DISPLAY=y
CONFIG_BT_CTLR_PHY_2M=n
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# Разрешает перезапись существующей привязки при повторном спаривании
# (подтягивает BT_SMP_ALLOW_UNAUTH_OVERWRITE). Без этого Zephyr отвергает
# Just Works-спаривание с хостом, для которого уже сохранён ключ, отвечая
# Pairing Failed / Authentication requirements (0x03), и требуется BT_CLR.
CONFIG_ZMK_BLE_EXPERIMENTAL_SEC=y
